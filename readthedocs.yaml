version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py
  output_dir: $READTHEDOCS_OUTPUT/html  # Utilisation du chemin correct

build:
  output_base: $READTHEDOCS_OUTPUT
