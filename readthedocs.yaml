version: 2

# Spécification des outils de construction
python:
  version: "3.10"  # Version de Python utilisée

# Installation des dépendances
python:
  install:
    - requirements: docs/requirements.txt

# Configuration de Sphinx
sphinx:
  configuration: docs/conf.py

# Build settings
build:
  output_base: $READTHEDOCS_OUTPUT
